# Scraping — notas

| Lib | Repo | Por que anotei |
|---|---|---|
| scrapegraphai | https://github.com/ScrapeGraphAI/Scrapegraph-ai | Web scraping com LLM + LangChain. Grafos tipo `SmartScraperGraph` extraem dados estruturados de páginas sem precisar de parser/CSS selector fixo — útil quando o layout do site varia ou muda. Requer `playwright install` depois de instalar. Candidato pro Ciclope (modo Afiliados) pra extrair preço/specs de páginas de produto automaticamente. |
